{
  "id": "mem9",
  "name": "Mnemo Memory",
  "description": "AI agent memory — server mode (mnemo-server). Hybrid vector + keyword search.",
  "kind": "memory",
  "configSchema": {
    "type": "object",
    "properties": {
      "apiUrl": {
        "type": "string",
        "description": "mnemo-server URL (server mode)"
      },
      "tenantID": {
        "type": "string",
        "description": "Tenant ID for v1alpha1 mem9s routes (preferred)"
      },
      "apiToken": {
        "type": "string",
        "description": "Legacy alias for tenantID (kept for backward compatibility)"
      },
      "userToken": {
        "type": "string",
        "description": "Legacy alias for tenantID"
      },
      "bootstrapped": {
        "type": "boolean",
        "description": "Set automatically after first startup. Do not set manually."
      }
    }
  },
  "uiHints": {
    "apiUrl": {
      "label": "Server URL",
      "placeholder": "https://your-server.example.com"
    },
    "tenantID": {
      "label": "Tenant ID",
      "placeholder": "uuid..."
    },
    "apiToken": {
      "label": "Legacy Tenant ID Alias",
      "placeholder": "uuid...",
      "sensitive": true
    },
    "userToken": {
      "label": "Legacy Tenant ID Alias",
      "placeholder": "uuid...",
      "sensitive": true
    }
  }
}
