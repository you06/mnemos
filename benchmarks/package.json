{
  "name": "mnemo-benchmarks",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:1a": "vitest run unit/backend.test.ts",
    "test:1b": "vitest run unit/hooks.test.ts"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "typescript": "^5.5.0"
  }
}
